# Intertwine

> Intertwine an Iterator of Iterators, yielding from whichever returns fastest.

This can be used to, for instance, combine many `IncomingConnections` iterators
into a single `Iterator` of connections.

